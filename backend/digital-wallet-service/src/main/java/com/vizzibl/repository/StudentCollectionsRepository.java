package com.vizzibl.repository;

import com.vizzibl.entity.StudentCollections;
import org.springframework.data.jpa.repository.JpaRepository;

public interface StudentCollectionsRepository extends JpaRepository<StudentCollections, Long> {
}
